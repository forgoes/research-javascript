import {tryForLoop, tryPromiseAll, tryReturnPromiseAll, tryCatchPromiseAll, tryMap, tryForEach, tryCatchPromiseAllTwo, trySyncForLoop} from "./parallel";

function main() {
    //tryForLoop()

    //tryPromiseAll()

    //tryReturnPromiseAll()

    //tryCatchPromiseAll()

    //tryMap()

    //tryForEach()

    //tryCatchPromiseAllTwo()

    trySyncForLoop()
}

main()
